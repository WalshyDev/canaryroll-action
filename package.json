{
  "name": "canaryroll-action",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action for CanaryRoll gradual deployments",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild --format=cjs --platform=node --bundle --outdir=dist src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest run"
  },
  "devDependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "@types/node": "^22.13.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
